services:
  - type: web
    name: iptv-panel
    runtime: python
    repo: https://github.com/josieljefferson/iptv-panel
    branch: main
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.auth:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
    healthCheckPath: /api/health
    autoDeploy: true